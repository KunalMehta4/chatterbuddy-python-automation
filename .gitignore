# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
env/

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Secrets: .env holds API keys and must never be committed.
.env

# Runtime state. The application creates these on first use; committing a
# developer's own contacts and tasks would be both noisy and a privacy problem.
data/*.json
data/*.corrupt-*
chatterbuddy.log

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store
